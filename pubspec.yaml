name: learn_dart_packages_application
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.16.1 <3.0.0'

publish_to: none # must publish to none if using git

dependencies:
   learn_dart_packages_library:
     git:
      url: https://github.com/haqiqiw/learn-dart-packages-library.git
      ref: 1.0.1 # branch or tag

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
